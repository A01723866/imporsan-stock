import React from 'react';
import ReactDOM from 'react-dom/client';
import '../../style.css';
import MovimientosPage from './page.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MovimientosPage />
  </React.StrictMode>,
);
