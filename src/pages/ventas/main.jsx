import React from 'react';
import ReactDOM from 'react-dom/client';
import '../../style.css';
import VentasPage from './page.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VentasPage />
  </React.StrictMode>,
);
