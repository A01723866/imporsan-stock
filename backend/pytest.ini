[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not integration"
markers =
    integration: requiere backend/.env y llamada real a Supabase (Spakio)
filterwarnings =
    ignore::DeprecationWarning
