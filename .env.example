# URL del backend al que el frontend hace fetch.
# En desarrollo local apunta al uvicorn que corre en el puerto 8000.
# En producción, cambiar al dominio público del backend.
VITE_API_URL=http://localhost:8000
